package org.example.service;

public class ServiceMaquina 
{
    public static void serviceCadastrarMaquina(String nome, String setor)
    {
        //Exec. 3 Nome
        if(nome.isBlank() || nome.isEmpty() || nome.equals(null))
        {
            return sout;
        }
    }
}
